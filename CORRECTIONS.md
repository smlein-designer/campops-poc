# Corrections & judgment calls log

Running note of anywhere the build deviated from the brief in `CLAUDE.md`, or required a judgment call. Per the brief: "this feeds back into the project's corrections log."

## Setup phase

1. **Scaffolded in place, not into a nested `campops-poc/` folder.** The brief's example command (`npm create vite@latest campops-poc -- --template react-ts`) names a project folder, but this repo's root (`CampOps/`) was already the intended project root (empty aside from `CLAUDE.md` and `reference/`). Scaffolded directly into the current directory instead of creating a redundant nested folder. `package.json`'s `name` field is set to `campops-poc`.

2. **shadcn init wrote generated theme into the wrong file.** `npx shadcn@latest init` auto-detects the project's Tailwind CSS entry point by scanning for a file that already contains `@import "tailwindcss"`. Because `reference/globals.css` (the spec file) already had that line, shadcn's CLI mistook it for the live stylesheet and merged its default gray/black theme into it, instead of `src/index.css`. Fixed by:
   - Restoring `reference/globals.css` to its original, untouched content.
   - Correcting `components.json`'s `tailwind.css` path from `reference/globals.css` to `src/index.css`.
   - Writing the CampOps token block directly into `src/index.css`.

3. **Swapped the order of the two `@import` statements in `src/index.css`.** The brief's exact block has `@import "tailwindcss";` before the Google Fonts `@import url(...)`. In Tailwind v4, `@import "tailwindcss"` expands into `@layer` statements, which pushes the font import into a position after non-import rules — invalid per the CSS spec (`@import` must precede all other rules). Tailwind's build tooling silently drops the font `@import` when ordered this way, so Fraunces/Public Sans never loaded. Fixed by putting the font import first. Verified empirically: fonts.googleapis.com URL is present in compiled output only with this order. `reference/globals.css` was left in its original order since it's the supplied spec artifact, not build output — this note documents the actual applied fix.

4. **Removed unused packages.** shadcn init's default Nova preset installed `@fontsource-variable/geist` and `tw-animate-css`, neither of which the CampOps token block uses (fonts come from the Google Fonts CDN import). Uninstalled both to keep the dependency list honest.

5. **Removed deprecated `baseUrl` from `tsconfig.json`/`tsconfig.app.json`.** TypeScript in this scaffold flagged `baseUrl` as deprecated (TS5101). The `@/*` path alias shadcn needs still resolves correctly with just `paths` and no `baseUrl` under `moduleResolution: "bundler"`.

6. **`vite.config.ts` uses `import.meta.dirname` instead of `__dirname`** for the `@` alias — Vite flagged `__dirname` as unsupported by its native config loader going forward.

7. **Fixed stale `<title>scaffold-tmp</title>`** in `index.html`, a leftover from the temporary folder name used during scaffolding, changed to `CampOps`.

## Component build phase

8. **Badge trimmed to exactly the four contract variants** (`secondary`, `destructive`, `warning`, `success`) via the same `cva` call shadcn's own Badge uses — not a separate one-off component. Real shadcn Badge ships `default`/`outline`/`ghost`/`link` too, but the brief is explicit ("Four variants total") and none of those four map to a severity/status meaning this design system needs, so they were dropped rather than left as unused dead variants. `defaultVariants` set to `secondary` (the neutral state) since there's no `default` variant anymore.

9. **Badge's `destructive` background switched from `bg-destructive/10` (shadcn's default opacity trick) to `bg-destructive-soft`**, matching the dedicated `--destructive-soft`/`--warning-soft`/`--success-soft` tokens the brief already defines. All three status variants now use the same soft-background pattern consistently.

10. **Badge's base radius changed from `rounded-4xl` to `rounded-full`.** shadcn's default Badge relies on a `--radius-4xl` theme key that only exists in shadcn's own generated Nova preset — the brief's exact token block doesn't define a radius scale beyond `--radius`/`--radius-sm`/`--radius-full`, so `rounded-4xl` had no value to resolve and badges rendered as sharp rectangles instead of pills. `rounded-full` is a static Tailwind utility (not theme-dependent) and matches the pill shape the design clearly intends. Button and Select still reference the same missing `--radius-md` key in their unused `xs`/`sm`/`icon-sm` size variants — left alone since the brief says CampOps doesn't use Button/Select size variants, so those code paths are inert, but flagging here in case that changes.

11. **FieldGroup and CheckboxRow don't auto-wire `id`/`aria-describedby` onto their children.** Considered cloning the child element to inject those automatically, but Input (a real DOM node) and Select (a compound Radix tree whose actual focusable element is `SelectTrigger`, several levels below the `Select` root) can't be handled the same way by a single `cloneElement` call. Chose the simpler, explicit pattern instead: the caller passes `id={htmlFor}` directly to the inner control. Less magic, easier to reason about, and works identically for both Input and Select.

12. **Removed the default Vite/React template's leftover assets** (`src/App.css`, `src/assets/*`, `public/icons.svg`) once `App.tsx` was rewritten and no longer referenced them. `public/favicon.svg` was kept since `index.html` still points to it and there's no CampOps-branded favicon yet.

13. **Badge status icons added** (`destructive` → `AlertCircle`, `warning` → `TriangleAlert`, `success` → `CircleCheck`, `secondary` → none), sourced from the spec in chat since this detail had fallen out of `CLAUDE.md`. Checked the installed `lucide-react` version (1.31.0) directly against its export list rather than assuming names hadn't shifted — confirmed `AlertCircle`/`TriangleAlert`/`CircleCheck` are current, not deprecated, aliases. Icons use no explicit color class so they inherit `currentColor` from each variant's existing text-color class — same token, no duplication. Sized at `size-3.5` (14px, the bottom of the given 14–16px range) since Badge's own text sits at `text-xs` (12px) and the tighter end reads better against it. This also meant changing the base badge SVG rule from the shadcn default `size-3!` to `size-3.5!`, since that rule now governs the icon that's core to the component rather than an optional decorative one.

**Radius fix follow-up:** confirmed — item 10 above hardcoded `rounded-full` directly on Badge's base class. No `--radius-4xl` (or any new token) was added to either `src/index.css` or `reference/globals.css`, so there's no drift between the two to reconcile.

## CampOps-specific compositions

14. **ReadinessItem's severity vocabulary is `"blocking" | "warning"`, not the Badge's `variant` names.** The brief's readiness table uses "Blocking"/"Warning" as the severity column, so `ReadinessItem` takes a `severity` prop in that vocabulary and maps it internally to the matching Badge variant (`blocking` → `destructive`, `warning` → `warning`) and display label (`Blocking`/`Advisory`). Kept the mapping inside the component rather than asking callers to pass a Badge variant directly, since "blocking"/"warning" is the actual domain language from the object model (`ValidationIssue`), not a styling detail callers should have to know.

15. **ReadinessPanel's heading copy ("Before you publish" / "Ready to publish") isn't in the brief.** The brief gives exact copy for each per-field validation message but not for the panel-level heading. Chose two short, neutral headings gated on `status`: `incomplete` → "Before you publish", both `ready-with-warnings` and `ready` → "Ready to publish" (matches the brief's own framing that warnings don't block publishing — screen 2's Publish button is enabled, so the panel shouldn't read as blocked). Flagging since this is invented copy, not sourced from the spec.

16. **ReadinessPanel background is unconditionally `bg-card` regardless of `status`.** Per the brief: "All three share the same white/card background, don't tint the warning state." No status-based background branching was written at all, so there's no tinted state to accidentally reintroduce later.

17. **PageHeader's title uses `text-[length:var(--display-sm)]` (arbitrary-value syntax) instead of a new `text-display-sm` utility class.** `src/index.css` already defines `--display-sm`/`--display-md`/`--display-lg` in `:root`, but they were never remapped into the `@theme inline` block, so no `text-display-*` Tailwind utility currently exists for them. Rather than editing `src/index.css` to add that mapping (a token-file change outside this component's scope), referenced the existing CSS variable directly via Tailwind's arbitrary-value syntax. Still satisfies "every ... value comes from a CSS variable" (rule 1) — no hardcoded size — but flagging that `--display-md`/`--display-lg` remain fully unused/unwired, in case a future screen needs them exposed as real utilities.

18. **PageHeader's breadcrumb is a `<button>`, not an `<a>`.** The brief calls it a "breadcrumb link" but this is a router-less POC with no navigation target to link to yet. Used `onBreadcrumbClick?: () => void` on a `<button>` styled with the `link` token so it's visually identical to a link; can be swapped for an anchor/router `Link` once real screen routing exists.

19. **PageHeader's `actions` prop is a single `React.ReactNode` slot, not a typed 1-2-button API.** The brief says "1-2 action Buttons" but doesn't specify props for them individually (labels, variants, handlers vary per screen). Rather than inventing an `actions: [{label, variant, onClick}]` array shape not in the contract, left it as a children-style slot the caller fills with 1-2 `<Button>` elements directly — no new prop shape invented, per rule 3.

## Design system reconciliation

20. **Found the actual master components for all three compositions in the DS Figma file** (`ReadinessItem`, `ReadinessPanel`, `PageHeader` each have their own page, reachable by direct node ID) and corrected the code to match. These pages didn't surface when the file was queried for its top-level page list — only "Style Guide" came back — so item 15's guess was made without the real source. Concrete corrections made against the canonical components:
    - **Item 15 was wrong.** `ReadinessPanel`'s heading is not dynamic — it's the static label **"Publish readiness"** in every status (`Incomplete`, `Ready-with-warnings`, `Ready`), styled Public Sans SemiBold `text-lg` (not Fraunces `text-base` as originally built). Fixed.
    - **The `ready` status has no item list at all.** It shows a single line instead: *"No issues found. This campsite is ready to publish."* (replaces the placeholder "All required information is complete." text that was invented without a source). Fixed.
    - **`ReadinessItem`'s bottom border belongs to the item itself**, not a `divide-y` wrapper — every item carries `border-b border-border`, including the last one in the list. Switched from `divide-y` to a per-item border to match. Also corrected internal spacing: `gap-4`/`items-center` (was `gap-3`/`items-start`).
    - **`PageHeader`'s layout is a single horizontal row**, not a `flex-col` wrapping everything: `title-block` (breadcrumb stacked tightly above the title, `gap-1`) sits on the left, actions on the right. Breadcrumb text is `text-xs` (12px) with a `size-3.5` icon, not `text-sm`/`size-4` as originally built. The title's font/size (`font-display`, `var(--display-sm)`) was already correct — confirmed by the source rather than guessed.
    - The master `PageHeader` symbol also confirmed the status-badge-slot sits inline in the title row next to the title text (`success` variant, `circle-check` icon, "Published" label) — matches the structure already built, no change needed there.

## Screen assembly

21. **`Campsite.publishStatus` modeled as `"draft" | "published"`, a binary, rather than one enum with a value per screen.** The brief frames the five screens as "matching the object model's `Campsite.publishStatus` states," but two of the five distinctions (missing-info vs. ready-with-warnings; published-clean vs. published-with-blocking-edits) are actually differences in *validation completeness*, not in publish state — both screens in each pair share the same `publishStatus`. Modeled `publishStatus` as the real persisted binary (has this site ever been published or not) and let `getReadinessStatus(issues)` — a pure function derived from the campsite's other fields — account for the completeness axis. This is what makes the two "published" screens (3 and 4) both carry `publishStatus: "published"` while differing only in whether the current draft edits reintroduced a blocking issue, matching the brief's own description of screen 4 ("was live, a later edit reintroduced a blocking issue").

22. **"One Campsite object" (brief) became five instances sharing one interface**, not literally one object. A single object can't simultaneously be in five different completeness/publish states, so `campsite-data.ts` defines the one `Campsite` shape plus one pure `getValidationIssues()` derivation function, and five mock instances (`campsiteMissingInfo`, `campsiteReadyWithWarnings`, `campsitePublished`, `campsitePublishedWithBlockingEdits`, and the public-preview screen reusing `campsitePublished`) built by spreading small deltas off the first one. Every screen's issue list is still derived from its instance's fields, never hardcoded — the part of the brief's instruction that actually matters ("keeping the object-model relationship honest") is intact.

23. **Fixed an inconsistency in the source Figma screen 1 mockup**: its `Site type` Select renders the placeholder "Select an item" (implying unset), but the screen's own `ReadinessPanel` never lists a missing-site-type issue — only "Max vehicle length" (blocking) and "Photos" (warning). Since issues are derived from the object rather than hardcoded per screen, an unset `siteType` would have produced a third blocking issue not present in the source. Set `siteType: "rv"` in the mock data (matching "RV site is missing max vehicle length," which presupposes RV is already selected) and render the Select as showing "RV site" — prioritizing internal consistency of the derivation over pixel-matching a mockup that was itself inconsistent.

24. **`Amenity.publiclyVisible` is bound directly to each `CheckboxRow`'s checked state** — no separate "enabled" flag invented. The brief's object model names exactly one boolean per amenity (`publiclyVisible`), and the editor screens show exactly one checkbox per amenity, so the checkbox toggles that field directly. The public preview screen filters to `publiclyVisible === true` amenities for its tag list, which is what makes the same six amenities in the editor produce exactly "Fire pit, Picnic table" in both the published-success summary and the public preview.

25. **The "Badge (severity)" instances on the public preview screen (site-type tag, amenity tags) are not the shared `Badge` component**, despite the Figma layer name. Their styling is a plain muted/neutral chip (`bg-muted`/`text-muted-foreground`) with no severity meaning — none of `Badge`'s four status variants (`destructive`/`warning`/`success`/`secondary`) matches that color, and forcing one in would misuse a severity-coded component for non-severity content. Built a small local `Tag` element scoped to `public-preview.tsx` instead, since it's a one-screen, non-recurring pattern rather than something worth promoting to a new shared primitive.

26. **`CampsiteEditorForm` extracted as a shared internal component** (`screens/campsite-editor-form.tsx`) used by three screens (missing-info, ready-with-warnings, blocking-edits) whose "Campsite details / Amenities / Availability & Restrictions" section is byte-identical in the source file across all three. Not exported from `components/`, since it's screen-assembly plumbing specific to this one editor flow, not a reusable design-system primitive.

27. **No router installed.** `App.tsx` switches between the five screens (plus the existing component showcase) via local `useState`, consistent with item 18's earlier call to skip routing for this router-less POC. `PageHeader`'s breadcrumb `onBreadcrumbClick` isn't wired to anything in these screens yet since there's nowhere to navigate to.

## Bug fixes (post-review)

28. **Two field groups in a shared flex row (e.g. Capacity / Max vehicle length) misaligned their inputs whenever one had helper/error text and the other didn't.** Default flex `align-items: stretch` stretched the shorter `FieldGroup` to match its sibling's height, and CSS Grid's default `align-content: stretch` then redistributed that extra space across the shorter grid's own rows — shifting its input down. Fixed in `campsite-editor-form.tsx` by adding `items-start` to all three field-group rows, so each `FieldGroup` keeps its natural height and every input lands at the same offset regardless of what's below it. User-reported.

## Design library update sync

29. **Badge: icon size increased 14px → 16px across all severity variants**, and a **new `neutral` variant was added** (`bg-muted`/`text-muted-foreground`, no icon), sourced from an update to the DS Badge page (`59:235`). This retroactively resolves item 25 — the local `Tag` element built for `public-preview.tsx` because no matching Badge variant existed at the time is now replaced with `<Badge variant="neutral">`, since the DS now defines that exact style as a real variant. Note this makes Badge a 5-variant component, which diverges from the original brief's "Four variants total: destructive/warning/success/secondary" — treating the newer, more specific DS update as authoritative over the earlier written brief, since Figma is the live source of truth and the two now visibly disagree (`secondary`'s earth-tint background is not the same color as the new `neutral`'s muted-gray background).

30. **PageHeader: title font size increased from `--display-sm` (24px, Display/H3) to `--display-md` (32px, Display/H2)**, and the outer row's alignment changed from `items-center` to `items-end`, sourced from an update to the DS PageHeader page (`33:120`).

31. **ReadinessPanel: heading font reverted from Public Sans back to Fraunces (display font)**, sourced from an update to the DS ReadinessPanel page (`20:19`/`20:18`) — this un-does the font-family half of item 20's earlier correction (the text size/weight stayed Public Sans SemiBold-equivalent at the time; the DS has since moved the whole heading to the display typeface). Confirms the DS is being actively iterated and today's "correct" reading can be superseded — worth periodically re-checking pages already reconciled rather than treating them as permanently settled.

32. **`Checkbox`'s checked-state styling never applied.** `checkbox.tsx` used Tailwind's `data-checked:` variant (matches a literal `data-checked` attribute), but Radix sets `data-state="checked"` — so `bg-primary`/`border-primary`/`text-primary-foreground` never fired on check, leaving checked boxes visually identical to unchecked ones (just a bare checkmark, no fill). This diverged from the DS master component (`61:30`, Checkbox/State=Checked): solid `primary` background, `primary` border, white check icon. Fixed by switching to `data-[state=checked]:`. User-reported, caught by comparing rendered checkboxes against the DS Figma page.

33. **Missing explicit line-heights on several screen-assembly headings.** User-reported: "Campsite details" (and every other section heading built directly in a screen file, not as one of the three verified compositions) rendered with visibly looser spacing than the mockup. Root cause: for `ReadinessItem`/`ReadinessPanel`/`PageHeader` I pulled exact styles from Figma via `get_design_context` before writing them; for the screen-assembly headings — "Campsite details," "Amenities," "Availability & Restrictions" (`campsite-editor-form.tsx`), "Campsite published successfully" (`published-success.tsx`), and "Amenities"/"Ready to book?"/"Things to know" (`public-preview.tsx`) — I wrote a plausible `text-lg font-semibold` / `text-base font-semibold` from visual memory and never checked. Checking now against the actual screen nodes (not just the style guide, which has an unrelated stale caption) confirmed font-family and size were already right, but line-height was never set, so Tailwind's default `text-lg`/`text-base`/`text-xl`/`text-2xl` line-heights applied instead of the spec's explicit values (`leading-[1.35]` for the 18px section headings, `leading-normal` for the rest). Fixed all seven. Also worth flagging, not fixed: `Label` (`ui/label.tsx`, stock shadcn) uses `leading-none` where the actual field-label nodes in Figma specify `leading-[normal]` — left alone since it's unmodified shadcn primitive structure per the brief's "only its colors changed, not its structure" rule for primitives, and the visual difference on a single-line 14px label is negligible; flagging in case that judgment should be revisited.

**Process note:** this class of bug (screen-assembly text styled from memory instead of verified against Figma) is specific to *stage 3* work — the three reusable compositions built in stage 2 were each checked individually. Any other one-off text in the five screens not covered by a `get_design_context` call during assembly should be treated as unverified until checked.

34. **Built a `Typography` primitive (`ui/typography.tsx`) to close the class of bug behind items 30–33, and documented the type scale + heading-level convention in `CLAUDE.md`.** No Figma Code Connect access on this project, so there's no automated binding between Figma's type styles and the code — every rung had to be read manually off the Style Guide page's actual node styles (not captions, per item 20's earlier finding that captions can be stale). Ten variants now exist as a fixed, verified table; new text should reference a variant instead of hand-writing `text-*`/`leading-*` combinations from memory.
    - Migrated the two headings that were **exact** matches to a named rung: `PageHeader`'s title (`display-h2`) and `ReadinessPanel`'s "Publish readiness" heading (`heading-h4`) — both confirmed against Figma with matching font, size, weight, *and* line-height.
    - Migrated `ReadinessItem`'s message/field text (`body-sm`/`caption`) and `ReadinessPanel`'s "ready" empty-state text (`body-sm`) — same exact-match confirmation.
    - **Did not** migrate `campsite-editor-form.tsx`'s three section headings ("Campsite details," "Amenities," "Availability & Restrictions") to `heading-h4`, despite matching size/weight/line-height exactly. Checking the actual screen nodes (not the Style Guide) shows they render in Public Sans, while the Style Guide's `Heading/H4` rung is Fraunces — confirmed visually in our own screenshots (serif "Publish readiness" vs. sans "Campsite details" on the same screen). This is a genuine inconsistency *inside the Figma file itself* — the abstract type-ramp token disagrees with what's actually applied to real screen instances sharing that same visual role. Kept these three as plain `text-lg leading-[1.35] font-semibold` (no named-rung match) rather than force-conforming to a token that doesn't match what's actually on the mockup. Worth raising with design — this isn't something to silently resolve from the code side.
    - Also added the missing `leading-[1.4]` to `PageHeader`'s breadcrumb text (Caption rung, same class of bug as the rest of this batch).

## DESIGN_SYSTEM.md split-off (post-audit)

35. **Split `CLAUDE.md` into `CLAUDE.md` (operating instructions) and `DESIGN_SYSTEM.md` (Figma-to-code implementation contract)** at user request, to stop the build brief from doubling as a design-system spec. Moved: component contracts/variants/states, naming conventions, token-usage rule, the full Typography section, and the validation-copy table. Kept in `CLAUDE.md`: project purpose, stack, setup steps, source-of-truth pointers, workflow/screen list, mock-data architecture, and four rules (inspect Figma before implementing, prefer reuse, consult `DESIGN_SYSTEM.md`, consult/maintain `CORRECTIONS.md`).

36. **Trimmed the Typography table in `DESIGN_SYSTEM.md`** — removed the Font/Size/Weight/Line-height columns (raw values already fully implemented in `typography.tsx`) and kept only Variant/Default tag/Figma rung name, per user direction: Figma owns the design values, `typography.tsx` owns the implementation, `DESIGN_SYSTEM.md` owns the *mapping* between them, not a second copy of the numbers. Without Code Connect, a duplicated value has no mechanism to stay in sync and just becomes a second thing to go stale.

37. **Third DS update: Badge gained a 0.5px border matching its text/icon color** on all four severity variants (`destructive`, `warning`, `success`, `neutral`), sourced from an update to the Badge, PageHeader, ReadinessItem, and ReadinessPanel DS pages (`59:235`, `33:120`, `20:2`, `20:17`) — previously borderless. `secondary` (earth-tint, no Figma counterpart — see item 29) was left untouched since there's no DS spec for it. Fixed in `ui/badge.tsx` alone — `ReadinessItem`, `ReadinessPanel`, and `PageHeader` all consume the shared `Badge` component already, so no changes were needed in those three files; this is exactly the payoff of routing every severity/status badge through one shared component instead of duplicating badge markup per screen.
